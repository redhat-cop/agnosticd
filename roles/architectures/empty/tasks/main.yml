---
- name: Perform {{ agnosticd_action }} for architecture
  ansible.builtin.include_tasks:
    file: "{{ agnosticd_action }}.yml"
  when: >-
    (role_path ~ '/tasks/' ~ agnosticd_action ~ '.yml') is exists
