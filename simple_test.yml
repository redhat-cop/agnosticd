---
- name: Simple Filter Test
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Test basic functionality
      debug:
        msg: "Basic test works"
    
    - name: Test if AWS filter is available
      debug:
        msg: "{{ {'test': 'value'} | aws_get_optimal_regions }}"
      ignore_errors: yes
