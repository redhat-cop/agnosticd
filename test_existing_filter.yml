---
- name: Test Existing Filter
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Test existing filter from core.py
      debug:
        msg: "{{ {'test': 'value'} | ec2_tags_to_dict }}"
      ignore_errors: yes
