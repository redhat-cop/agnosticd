apiVersion: apps.3scale.net/v1alpha1
kind: APIManager
metadata:
  name: "{{ _3scale_apimanager_instancename }}"
  namespace: 3scale
spec:
  wildcardDomain: "{{ ocp_apps_domain }}"
  resourceRequirementsEnabled: false
  system:
    fileStorage:
      persistentVolumeClaim:
        storageClassName: "{{ _nfs_provided_storage_class }}"
    database:
      postgresql: {}