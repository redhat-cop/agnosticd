---
# defaults file for containerlab

containerlab_pip_dependencies:
  - ansible-pylibssh
  - ansible-lint

containerlab_lab_username: "{{ student_name | default('student') }}"

# Valid url of the image tar
# containerlab_arista_ceos_image_tar_url: https://<changeme>.example.com/image.tar
