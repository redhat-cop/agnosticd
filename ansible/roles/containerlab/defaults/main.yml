---
# defaults file for containerlab

containerlab_pip_dependencies:
  - ansible-pylibssh
  - ansible-lint

containerlab_lab_username: "{{ student_name | default('student') }}"

# ---------------------------------------------
# Required: url for container image tar and
# for ansible collection tar
# ---------------------------------------------
# containerlab_arista_ceos_image_tar_url: https://<changeme>/cEOS64-lab-4.32.0F.tar.tar
# containerlab_ansible_collections_tar_url: https://<changeme>//collections.tar.gz
