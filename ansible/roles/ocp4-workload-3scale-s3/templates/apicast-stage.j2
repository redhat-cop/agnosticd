apiVersion: apps.3scale.net/v1alpha1
kind: APIcast
metadata:
  name: "stage-apicast"
  namespace: "{{tenantGwProject}}"
spec:
  adminPortalCredentialsRef:
    name: "apicast-configuration-url-secret-stage"
  replicas: 1
  configurationLoadMode: lazy
  deploymentEnvironment: sandbox