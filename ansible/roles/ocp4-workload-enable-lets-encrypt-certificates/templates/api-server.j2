apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  servingCerts:
    namedCertificates:
    - names:
      - {{ api_hostname.stdout }}
      servingCertificate:
        name: api-certs