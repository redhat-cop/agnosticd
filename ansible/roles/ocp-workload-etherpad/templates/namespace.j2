apiVersion: v1
kind: Namespace
metadata:
  annotations:
    openshift.io/description: ""
    openshift.io/display-name: "{{ etherpad_project_display }}"
    openshift.io/requester: "{{ ocp_username }}"
  name: "{{ etherpad_project }}"
spec:
  finalizers:
  - kubernetes
