apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: amq-streams
  namespace: '{{ operators_project }}'
spec:
  channel: stable
  installPlanApproval: Automatic
  name: amq-streams
  source: redhat-operators-snapshot-dil
  sourceNamespace: {{ snapshot_operator_project }}