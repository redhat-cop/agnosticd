---
apiVersion: maistra.io/v1
kind: ServiceMeshMemberRoll
metadata:
  name: default
  namespace: "{{ project }}"
spec:
  members:
    - "{{ user }}-inventory"
    - "{{ user }}-catalog"
    - "{{ user }}-bookinfo"
    - "{{ user }}-cloudnativeapps"
