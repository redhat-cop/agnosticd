---
# List of dictionaries describing what roles to run on which hosts.
# Each entry must have `name`, should have `hosts`, and may also set `vars`.
host_workloads: []
host_workloads_become: "{{ inventory_hostname != 'localhost' }}"
...
