---

- name: Start_webserver
  service:
    name: "{{ a_simple_webserver_service }}"
    state: started

- name: Reload_webserver
  service:
    name: "{{ a_simple_webserver_service }}"
    state: reloaded

...
