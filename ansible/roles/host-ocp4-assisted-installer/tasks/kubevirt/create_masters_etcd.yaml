---
- name: Create Master virtual machine
  kubevirt.core.kubevirt_vm:
    host: "{{ openshift_cnv_api_uri }}"
    api_key: "{{ k8s_auth_results.k8s_auth.api_key }}"
    validate_certs: false
    name: "{{ vmname }}"
    namespace: "{{ namespace }}"
    data_volume_templates:
      - metadata:
          name: "{{ vmname }}-installation-cdrom"
        spec:
          source:
            http:
              url: "{{ image_url }}"
          storage:
            resources:
              requests:
                storage: 2Gi
      - metadata:
          name: "{{ vmname }}"
        spec:
          preallocation: false
          source:
            blank: {}
          storage:
            resources:
              requests:
                storage: 100Gi
            storageClassName: "{{ storageclass }}"

      - metadata:
          name: "{{ vmname }}-etcd"
        spec:
          preallocation: false
          source:
            blank: {}
          storage:
            resources:
              requests:
                storage: 30Gi
            storageClassName: "{{ ai_etcd_storage_class }}"
            volumeMode: Filesystem

    running: true
    wait: true
    wait_timeout: 600
    annotations:
      vm.kubevirt.io/os: rhel8
    labels:
      kubevirt.io/domain: "{{ vmname }}"
      role: control-plane
    spec:
      domain:
        cpu:
          cores: 8
          sockets: 1
          threads: 1
        devices:
          disks:
            - bootOrder: 1
              disk:
                bus: virtio
              name: rootdisk
            - bootOrder: 3
              disk:
                bus: virtio
              name: etcd
            - bootOrder: 2
              cdrom:
                bus: sata
              name: installation-cdrom
          interfaces:
            - masquerade: {}
              model: virtio
              name: default
            - name: "{{ network }}"
              macAddress: "{{ ai_masters_macs[_index|int] }}"
              bridge: {}
          networkInterfaceMultiqueue: true
        machine:
          type: pc-q35-rhel8.6.0
        resources:
          requests:
            memory: 16Gi
      evictionStrategy: LiveMigrate
      readinessProbe:
        httpGet:
          port: 6443
        initialDelaySeconds: 120
        periodSeconds: 5
      networks:
        - name: default
          pod:
            vmNetworkCIDR: "{{ pod_network }}"
        - name: "{{ network }}"
          multus:
            networkName: "{{ network_name }}"
      terminationGracePeriodSeconds: 180
      volumes:
        - dataVolume:
            name: "{{ vmname }}"
          name: rootdisk
        - dataVolume:
            name: "{{ vmname }}-installation-cdrom"
          name: installation-cdrom
        - dataVolume:
            name: "{{ vmname }}-etcd"
          name: etcd
