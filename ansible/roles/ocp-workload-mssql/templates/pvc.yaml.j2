---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ mssql_pvc_name }}
  namespace: {{ mssql_namespace }}
  labels:
    app: {{ mssql_app_name }}
spec:
  storageClassName: "{{ mssql_storage_class }}"
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
