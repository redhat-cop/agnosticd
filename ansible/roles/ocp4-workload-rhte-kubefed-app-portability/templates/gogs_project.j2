apiVersion: project.openshift.io/v1
kind: Project
metadata:
  annotations:
    openshift.io/description: ""
    openshift.io/display-name: "{{ _gogs_project_display }}"
  name: "{{ _gogs_project }}"
spec:
  finalizers:
  - kubernetes
