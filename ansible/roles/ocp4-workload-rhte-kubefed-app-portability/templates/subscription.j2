apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: kubefed
  namespace: "{{ _kubefed_project }}"
spec:
  channel: alpha
  package: kubefed-operator
  installPlanApproval: Manual
  name: kubefed-operator
  startingCSV: "{{ _kubefed_subscription_csv }}"
  source: "installed-kubefed-{{ _kubefed_project }}"
  sourceNamespace: "{{ _kubefed_project }}"
