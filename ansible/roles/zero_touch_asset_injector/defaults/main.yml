---

# -------------------------------------------------
# Automation Controller Issue cert
# -------------------------------------------------
zero_touch_asset_injector_automation_controller_issue_cert: false
zero_touch_asset_injector_automation_controller_fqdn: "{{ ansible_host.split('.')[0] | lower }}.{{ subdomain_base }}"
zero_touch_asset_injector_automation_controller_username: admin
zero_touch_asset_injector_automation_controller_password: "CHANGEME"
# Define when required
# zero_touch_asset_injector_automation_controller_nginx_https_port: 8443
