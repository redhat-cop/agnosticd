kind: ConfigMap
apiVersion: v1
metadata:
  name: app-config
data:
  app-config.yaml: |-
    catalog.http.port: 8080
    connection_string: mongodb://catalog-mongodb:27017
    db_name: {{ catalog_database }}
    username: {{ catalog_db_user }}
    password: {{ catalog_db_password }}