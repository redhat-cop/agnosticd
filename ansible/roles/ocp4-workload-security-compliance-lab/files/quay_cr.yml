apiVersion: cop.redhat.com/v1alpha1
kind: QuayEcosystem
metadata:
  name: example-quayecosystem
spec:
  imagePullSecretName: coreos-pull-secret

  quay:
    registryStorage:
        persistentVolume:
          accessModes:
          - ReadWriteOnce
          capacity: 100Gi