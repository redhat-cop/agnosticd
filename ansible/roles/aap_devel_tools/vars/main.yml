---
# vars file for aap_devel_tools
aap_devel_tools_repos:
  - ansible-automation-platform-2.2-for-rhel-8-x86_64-rpms
aap_devel_tools_packages:
  - ansible-core  # just to reduce risk to pull ansible by default
  - ansible-navigator
  - ansible-builder
  - ansible-test
  - ansible-lint
