---
tower_custom_virtualenvs:
  - dest: /opt/rh/virtualenvs
    name: ansible-2.9.3
    requirements:
      - ansible==2.9.3

  - dest: /opt/rh/virtualenvs
    name: ansible-latest
    requirements:
      - ansible
