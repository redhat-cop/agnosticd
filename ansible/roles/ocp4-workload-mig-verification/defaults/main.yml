mig_verify_expected_ns:
- openshift-migration
- openshift-storage
# more namespaces to come (demo apps)

mig_namespace: openshift-migration

mig_demo_apps_ns:
- mssql-persistent
- sock-shop
- robot-shop

silent: false
