---
- debug: msg="ansible_user is {{ ansible_user }}"
- debug: msg="requester_email is {{ requester_email }}"
- debug: msg="email is {{ email }}"
- debug: msg="user is {{ user }}"
- debug: msg="requester_username is {{ requester_username }}"
- debug: msg="ocp_username is {{ ocp_username }}"

- name: pre_workload Tasks Complete
  debug:
    msg: "Pre-Software checks completed successfully"
