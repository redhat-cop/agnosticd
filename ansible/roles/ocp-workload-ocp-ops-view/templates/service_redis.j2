apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ opsview_app_name }}-redis
  name: {{ opsview_app_name }}-redis
  namespace: {{ opsview_project }}
spec:
  selector:
    app: {{ opsview_app_name }}-redis
  type: ClusterIP
  ports:
  - port: 6379
    protocol: TCP
    targetPort: 6379