apiVersion: project.openshift.io/v1
kind: Project
metadata:
  annotations:
    openshift.io/description: ""
    openshift.io/display-name: "{{ opsview_project_display }}"
    openshift.io/requester: "{{ ocp_username }}"
  name: "{{ opsview_project }}"
spec:
  finalizers:
  - kubernetes
