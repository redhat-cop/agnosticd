apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ opsview_app_name }}
  name: {{ opsview_app_name }}
  namespace: {{ opsview_project }}
spec:
  selector:
    app: {{ opsview_app_name }}
  type: ClusterIP
  ports:
  - port: 8080
    protocol: TCP
targetPort: 8080