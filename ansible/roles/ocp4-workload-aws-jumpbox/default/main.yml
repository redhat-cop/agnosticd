---
become_override: false
ocp_username: opentlc-mgr
silent: false

# Variables defined from AgnosticD Runs:
# guid
# cluster_name
# aws_region
#
# When running this workload outside of a 
# full provisioning run pass variables via
# -e on the command line
