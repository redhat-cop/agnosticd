apiVersion: apps.3scale.net/v1alpha1
kind: APIManager
metadata:
  name: staging
spec:
  wildcardDomain: apps.cluster-2d1a.green.osp.opentlc.com
  resourceRequirementsEnabled: false
  system:
    fileStorage:
      persistentVolumeClaim:
        storageClassName: nfs-storage-type
    database:
      postgresql: {}