---
- import_tasks: locate_template.yml

- import_tasks: generate_aws_template.yml
  when: cloud_provider == 'aws'

- import_tasks: generate_osp_template.yml
  when: cloud_provider == 'osp'

# TODO: This could also be made simpler and not even require a conditional:
# - import_tasks: generate_{{ cloud_provider }}_template.yml

# TODO: This should have a fail task also in case there is no template for a cloud_provider