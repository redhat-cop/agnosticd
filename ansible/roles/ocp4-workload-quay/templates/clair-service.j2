apiVersion: v1
kind: Service
metadata:
  namespace: "{{ _quay_project }}"
  name: quay-clair
  labels:
    quay-component: clair
spec:
  ports:
    - port: 6060
      name: clair
    - port: 6061
      name: clair1
  selector:
    quay-component: clair
