apiVersion: v1
kind: Service
metadata:
  namespace: "{{ _quay_project }}"
  name: quay-postgres
  labels:
    quay-component: postgres
spec:
  ports:
    - port: 5432
  selector:
    quay-component: postgres
