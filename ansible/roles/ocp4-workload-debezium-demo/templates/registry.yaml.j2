---
apiVersion: apicur.io/v1alpha1
kind: ApicurioRegistry
metadata:
  name: service-registry
spec:
  configuration:
    persistence: "streams"
    streams: 
      bootstrapServers: 'demo-kafka-bootstrap:9092'
      applicationId: "service-registry"
  deployment:
    host: "service-registry-{{ ocp4_workload_debezium_demo_registry_namespace }}.{{ route_subdomain }}"
