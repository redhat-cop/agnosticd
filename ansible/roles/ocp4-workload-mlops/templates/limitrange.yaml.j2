---
kind: LimitRange
apiVersion: v1
metadata:
  name: cpu-limit-range
  namespace: "{{name}}"
spec:
  limits:
    - type: Container
      default:
        cpu: 1
        memory: 2Gi
      defaultRequest:
        cpu: 100m
        memory: 256Mi
