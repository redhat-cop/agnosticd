apiVersion: project.openshift.io/v1
kind: Project
metadata:
  annotations:
    openshift.io/description: ""
    openshift.io/display-name: "{{ _terminal_project_display }}"
  name: "{{ _terminal_namespace }}"
spec:
  finalizers:
  - kubernetes
