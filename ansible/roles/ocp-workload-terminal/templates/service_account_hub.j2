apiVersion: v1
kind: ServiceAccount
metadata: 
  name: "{{ _terminal_name }}-{{ _terminal_project }}-hub"
  namespace: "{{ _terminal_project }}"
  labels: 
    app: "{{ _terminal_name }}"
    spawner: terminal-server
    class: spawner