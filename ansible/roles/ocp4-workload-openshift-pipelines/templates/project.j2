apiVersion: project.openshift.io/v1
kind: Project
metadata:
  annotations:
    openshift.io/description: ""
    openshift.io/display-name: "{{ _pipelines_project_display }}"
  name: "{{ _pipelines_project }}"
spec:
  finalizers:
  - kubernetes
