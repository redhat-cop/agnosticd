apiVersion: operators.coreos.com/v1
kind: CatalogSourceConfig
metadata:
  name: installed-openshift-pipelines
  namespace: openshift-marketplace
spec:
  targetNamespace: "{{ _pipelines_project }}"
  packages: openshift-pipelines-operator