---
become_override: False
ocp_username: user-redhat.com
silent: False
tmp_dir: /tmp/{{ guid }}
tmp_kubeconfig: "{{ tmp_dir }}/.kube/config"

gogs_project: "gogs"
gogs_app_name: "gogs"
gogs_admin_user: gogs
gogs_admin_password: gogs
gogs_password: gogs
gogs_deploy_retry_count: 30
gogs_deploy_retry_delay: 10

# AS A USER, YOU SHOULD PROVIDE THE VARS BELOW
repository_base: https://github.com/openshift-roadshow
repositories:
  - nationalparks
  - nationalparks-py
  - nationalparks-php
  - nationalparks-js
