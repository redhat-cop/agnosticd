apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: {{ gogs_app_name }}
  name: {{ gogs_app_name }}
  namespace: {{ gogs_project }}
spec:
  host: {{ gogs_hostname }}
  to:
    kind: Service
    name: {{ gogs_app_name }}
    weight: 100
  wildcardPolicy: None