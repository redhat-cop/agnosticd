apiVersion: project.openshift.io/v1
kind: Project
metadata:
  annotations:
    openshift.io/description: ""
    openshift.io/display-name: "{{ _asb_broker_project_display }}"
  name: "{{ _asb_broker_project }}"
spec:
  finalizers:
  - kubernetes
