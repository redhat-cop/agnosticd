---
become_override: False
ocp_username: opentlc-mgr
silent: False

_asb_broker_project: openshift-automation-broker
_asb_broker_project_display: "OpenShift Automation Broker"
_asb_broker_subscription_csv: "openshiftansibleservicebroker.v4.1.2"

# One of the following needs to be True

# True to install the default APBs
_asb_install_default_apbs: False

# True to install local APBs
_asb_install_local_apbs: False

# True to install all APB images on Dockerhub
_asb_install_all_docker_apbs: False

# Install APBs from Quay:
# True to install BU Workshop APBs from Quay
# This does not seem to work at the moment
_asb_install_workshop_apbs_quay: False
_asb_workshop_apb_org_quay: "openshiftlabs"

# Install APBs from DockerHub:
# False to not install BU Workshop APBs from DockerHub
_asb_install_workshop_apbs_docker: True
_asb_workshop_apb_org_docker: "openshiftapb"

# Tag for BU Workshop APBs
_asb_workshop_apb_tag: "ocp-4.1"
