{
{% for instance in r_instances_info.instances %}
        {% if instance.tags.AnsibleGroup != 'bastions' %}{{ "\n" }}
            "{{ instance.tags.short_name }}.example.com": {
                "visible": true,
                "color": "{{ instance.tags.color }}",
                "user": "ec2-user",
                "address": "{{ instance.tags.short_name }}.example.com"
            }{% if not loop.last %},{% endif %}
        {% endif %}
{% endfor %}{{ "\n" }}
}
