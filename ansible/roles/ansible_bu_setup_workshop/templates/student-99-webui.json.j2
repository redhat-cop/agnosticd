{
    {% for instance in r_instances.instances|json_query('[?tags.name == `tidy-bengal`].tags') %}
      "{{ instance.short_name }}" : {
        "visible" : true,
        "color" : "#67d300",
        "user" : "ec2-user",
        "address" : "{{ instance.short_name }}"
    {% endfor %}
    },
    {% for instance in r_instances.instances|json_query('[?tags.name == `strong-hyena`].tags') %}
      "{{ instance.short_name }}" : {
        "visible" : true,
        "color" : "#67d300",
        "user" : "ec2-user",
        "address" : "{{ instance.short_name }}"
    {% endfor %}
    },
    {% for instance in r_instances.instances|json_query('[?tags.name == `more-calf`].tags') %}
      "{{ instance.short_name }}" : {
        "visible" : true,
        "color" : "#67d300",
        "user" : "ec2-user",
        "address" : "{{ instance.short_name }}"
    {% endfor %}
    },
    {% for instance in r_instances.instances|json_query('[?tags.name == `upward-moray`].tags') %}
      "{{ instance.short_name }}" : {
        "visible" : true,
        "color" : "#67d300",
        "user" : "ec2-user",
        "address" : "{{ instance.short_name }}"
    {% endfor %}
    }
}
