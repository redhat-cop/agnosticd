{
    {% for instance in r_instances_info.instances|json_query('[?tags.Name == `tidy-bengal`].tags') %}
      "{{ instance.short_name }}.example.com": {
        "visible": true,
        "color": "#d48a00",
        "user": "ec2-user",
        "address": "{{ instance.short_name }}.example.com"
    {% endfor %}
    },
    {% for instance in r_instances_info.instances|json_query('[?tags.Name == `strong-hyena`].tags') %}
      "{{ instance.short_name }}.example.com": {
        "visible": true,
        "color": "#0039d4",
        "user": "ec2-user",
        "address": "{{ instance.short_name }}.example.com"
    {% endfor %}
    },
    {% for instance in r_instances_info.instances|json_query('[?tags.Name == `more-calf`].tags') %}
      "{{ instance.short_name }}.example.com": {
        "visible": true,
        "color": "#bf00d4",
        "user": "ec2-user",
        "address": "{{ instance.short_name }}.example.com"
    {% endfor %}
    },
    {% for instance in r_instances_info.instances|json_query('[?tags.Name == `upward-moray`].tags') %}
      "{{ instance.short_name }}.example.com": {
        "visible": true,
        "color": "#67d300",
        "user": "ec2-user",
        "address": "{{ instance.short_name }}.example.com"
    {% endfor %}
    }
}
