apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: openshift-logging-operatorgroup
  namespace: openshift-logging
spec:
  targetNamespaces:
  - openshift-logging
status:
  namespaces:
  - openshift-logging