apiVersion: syndesis.io/v1alpha1
kind: Syndesis
metadata:
  name: app
  namespace: {{user_fuse_project}}
spec:
  integration:
    limit: 0
  components:
    server:
      features:
        managementUrlFor3scale: https://{{ user_3scale_project }}.{{ route_subdomain }}
addons:
  todo:
    enabled: 'false'
