apiVersion: project.openshift.io/v1
kind: Project
metadata:
  annotations:
    openshift.io/description: ""
    openshift.io/display-name: "{{ _minio_project_display }}"
  name: "{{ _minio_project }}"
spec:
  finalizers:
  - kubernetes
