---

# -------------------------------------------------
# Default Variables
# -------------------------------------------------
novnc_version: 1.4.0
novnc_enable_letsencrypt_cert: true
novnc_host_fqdn: "{{ groups['bastions'][0].split('.')[0] }}.{{ subdomain_base }}"
