---
- name: Subscription list 
  shell: hammer --output cve subscription list --organization "{{ORG}}"
  register: subscription_list
  tags:
    - satellite_configuration
    - manifest

- name: Include Manifest if not uploaded already
  include: 01_manifest.yml
  when: subscription_list.stdout_lines.2.split(":").1.lstrip(' ') != SUBSCRIPTION_NAME
  tags:
    - satellite_configuration
    - manifest

- include: 02_repository.yml
# - include: 03_content_view.yml
# - include: 04_lifecycle.yml
# - include: 05_activationkey.yml


