= ocp_cleanup_failed_pods

Cleanup failed pods and jobs on cluster.
The failed pods should automatically be restarted as needed and controlling cronjobs should create replacement jobs.

== Configuration

none
