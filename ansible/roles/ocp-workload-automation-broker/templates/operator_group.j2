apiVersion: v1
kind: List
items:
- apiVersion: operators.coreos.com/v1alpha2
  kind: OperatorGroup
  metadata:
    annotations:
      olm.providedAPIs: ""
    generateName: automation-broker-
    namespace: "{{ broker_project }}"
  spec:
    selector: {}
    serviceAccount:
      metadata:
    targetNamespaces:
    - "{{ broker_project }}"
