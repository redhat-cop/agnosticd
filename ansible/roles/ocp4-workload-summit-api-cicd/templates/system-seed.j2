kind: Secret
apiVersion: v1
metadata:
  name: system-seed
  namespace: "{{ API_MANAGER_NS }}"
  labels:
    app: 3scale-api-management
    threescale_component: system
    threescale_component_element: system
type: Opaque
stringData:
  TENANT_NAME: "{{ default_tenant_name }}"
  MASTER_DOMAIN: master
  MASTER_USER: master
  ADMIN_PASSWORD: "{{ amp_admin_passwd }}"
  ADMIN_ACCESS_TOKEN: "{{ default_tenant_access_token }}"
  MASTER_PASSWORD: "{{ amp_master_passwd }}"
  MASTER_ACCESS_TOKEN: "{{ master_access_token }}"
  ADMIN_USER: "{{ default_tenant_username }}"
  ADMIN_EMAIL: ""
