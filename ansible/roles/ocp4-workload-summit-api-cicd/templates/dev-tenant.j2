apiVersion: capabilities.3scale.net/v1alpha1
kind: Tenant
metadata:
  name: "{{ dev_tenant }}"
  namespace: "{{ API_MANAGER_NS }}"
spec:
  email: 3scaleadmin@redhat.com
  masterCredentialsRef:
    name: system-seed
  organizationName: "{{ dev_tenant }}"
  passwordCredentialsRef:
    name: "{{ dev_tenant }}-admin-secret"
  systemMasterUrl:  "https://master.{{ ocp_apps_domain }}"
  tenantSecretRef:
    name: "{{ dev_tenant }}-tenant-secret"
    namespace: "{{ API_MANAGER_NS }}"
  username: "{{ dev_tenant_admin }}"

