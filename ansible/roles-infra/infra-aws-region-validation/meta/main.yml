---
# ============================================================================
# AWS Region Validation Role Metadata
# ============================================================================

galaxy_info:
  author: AgnosticD Team
  description: Smart AWS region validation and selection based on workload requirements
  license: Apache-2.0
  min_ansible_version: 2.9
  platforms:
    - name: EL
      versions:
        - all
  galaxy_tags:
    - aws
    - region
    - validation
    - cloud
    - infrastructure

dependencies: []

# Role should run early in the deployment process
allow_duplicates: false
