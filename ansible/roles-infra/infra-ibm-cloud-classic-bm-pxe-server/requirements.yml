---
# Ansible collection requirements for infra-ibm-cloud-classic-bm-pxe-server role
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: ansible.posix
    version: ">=1.3.0"
  - name: community.general
    version: ">=4.0.0" 