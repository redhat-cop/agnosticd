{
  "name": "{{ guid }}-workspace",
  "type": [
    "terraform_v{{ terraform_version }}"
  ],
  "description": "Workspace for {{ guid }}",
  "resource_group": "{{ guid }}-rg",
  "location": "{{ region }}",
  "tags": [],
  "template_repo": {
    "url": ""
  },
  "template_data": [
    {
      "folder": ".",
      "type": "terraform_v{{ terraform_version }}"
    }
  ]
}
