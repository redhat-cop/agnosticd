- name: Dummy Playbook
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Output Info Message
      ansible.builtin.debug:
        msg: "Will be overwritten later - needed for master playbook to run"
