---
- name: Stop
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Entering the stop.yml
      ansible.builtin.debug:
        msg: Entering the stop.yml

    - name: Exiting the stop.yml
      ansible.builtin.debug:
        msg: Exiting the stop.yml
...
