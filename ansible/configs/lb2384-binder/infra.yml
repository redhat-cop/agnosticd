---
- name: Step 001 infra
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tags:
    - step001
    - infrastructure
  tasks:
    - name: Entering the infra.yml
      ansible.builtin.debug:
        msg: Entering the infra.yml

    - name: Exiting the infra.yml
      ansible.builtin.debug:
        msg: Exiting the infra.yml
...
