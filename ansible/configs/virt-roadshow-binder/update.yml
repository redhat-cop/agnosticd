---
- name: Update virt-roadshow-binder
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Entering the virt-roadshow-binder update.yml
      debug:
        msg:
          - Entering the virt-roadshow-binder update.yml

    - name: Exiting the virt-roadshow-binder update.yml
      debug:
        msg:
          - Exiting the virt-roadshow-binder update.yml
