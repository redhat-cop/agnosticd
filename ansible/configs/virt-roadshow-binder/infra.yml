---
- name: Step 001 infra
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tags:
    - step001
    - infrastructure
  tasks:
    - name: Entering the virt-roadshow-binder infra.yml
      debug:
        msg:
          - Entering the virt-roadshow-binder infra.yml

    - name: Exiting the virt-roadshow-binder infra.yml
      debug:
        msg:
          - Exiting the virt-roadshow-binder infra.yml
