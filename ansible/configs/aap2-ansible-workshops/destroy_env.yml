---
- name: Delete Infrastructure
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  vars_files:
    - "./env_vars.yml"

- import_playbook: "{{ playbook_dir }}/workdir/{{ env_type }}/provisioner/teardown_lab.yml"
