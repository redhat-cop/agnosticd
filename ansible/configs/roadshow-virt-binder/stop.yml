---
- name: Stop roadshow-binder
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Entering the roadshow-binder stop.yml
      debug:
        msg:
          - Entering the roadshow-binder stop.yml

    - name: Exiting the roadshow-binder stop.yml
      debug:
        msg:
          - Exiting the roadshow-binder stop.yml
