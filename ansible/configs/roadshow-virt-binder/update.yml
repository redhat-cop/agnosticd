---
- name: Update roadshow-binder
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Entering the roadshow-binder update.yml
      debug:
        msg:
          - Entering the roadshow-binder update.yml

    - name: Exiting the roadshow-binder update.yml
      debug:
        msg:
          - Exiting the roadshow-binder update.yml
