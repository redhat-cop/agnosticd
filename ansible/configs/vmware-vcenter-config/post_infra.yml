---
- name: Step 002 Post Infrastructure
  hosts: localhost
  gather_facts: false
  become: false
  tasks:
    - name: Step 002
      ansible.builtin.debug:
        msg: "Step 002 Post Infrastructure"
