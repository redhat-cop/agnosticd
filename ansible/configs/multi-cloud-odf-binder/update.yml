---
- name: Update multi-cloud-odf-binder
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Entering the multi-cloud-odf-binder update.yml
      debug:
        msg:
          - Entering the multi-cloud-odf-binder update.yml

    - name: Exiting the multi-cloud-odf-binder update.yml
      debug:
        msg:
          - Exiting the multi-cloud-odf-binder update.yml
...
