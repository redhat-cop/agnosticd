---
- name: Update service-interconnect-binder
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Entering the service-interconnect-binder update.yml
      debug:
        msg:
          - Entering the service-interconnect-binder update.yml

    - name: Exiting the service-interconnect-binder update.yml
      debug:
        msg:
          - Exiting the service-interconnect-binder update.yml
...
