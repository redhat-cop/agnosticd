---

- name: Step 003 Pre Software Validation
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  tags:
    - step003
    - pre_software_validation

  tasks:

    - name: Step 003 Pre Software Validation
      ansible.builtin.debug:
        msg: Validate Pre Software

...
