---

- name: Step 000 Post Software Validation
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  tags:
    - step000
    - post_software_validation

  tasks:

    - name: Step 000 Post Software Validation
      ansible.builtin.debug:
        msg: Validate Post Software Validation

...
