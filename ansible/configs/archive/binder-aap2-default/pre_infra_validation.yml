---

- name: Step 000 Pre Infrastructure Validation
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  tags:
    - step000
    - pre_infrastructure

  tasks:

    - name: Step 000 pre_infrastructure_validation
      ansible.builtin.debug:
        msg: Validate pre_infrastructure

    - name: "Pre_infrastructure checks completed successfully"
      ansible.builtin.debug:
        msg: "Pre_infrastructure checks completed successfully"
...
