---

- name: Step 000 post Infrastructure Validation
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  tags:
    - step002
    - post_infrastructure

  tasks:

    - name: Step 002 post Infrastructure Validation
      ansible.builtin.debug:
        msg: Validate post_infrastructure

    - name: "Post_infrastructure checks completed successfully"
      ansible.builtin.debug:
        msg: "Post_infrastructure checks completed successfully"
