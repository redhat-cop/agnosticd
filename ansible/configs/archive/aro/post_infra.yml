---
- name: Step 000 Post Infrastructure
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tags:
    - step001
    - post_infrastructure
  tasks:
    - debug:
        msg: "Step 000 Post Infrastructure - Dummy action"
