---
- name: Destroy playbook
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  tasks:

  - name: Entering the vld-pattern-base-binder destroy.yml
    debug:
      msg:
      - Entering the destroy.yml


  - name: Exiting the vld-pattern-base-binder destroy.yml
    debug:
      msg:
      - Exiting the destroy.yml
...
