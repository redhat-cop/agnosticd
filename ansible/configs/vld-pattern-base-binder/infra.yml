---
- name: Step 001 infra
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tags:
    - step001
    - infrastructure
  tasks:

    - name: Entering the vld-pattern-base-binder infra.yml
      debug:
        msg:
          - Entering the vld-pattern-base-binder infra.yml

    - name: Exiting the vld-pattern-base-binder infra.yml
      debug:
        msg:
          - Exiting the vld-pattern-base-binder infra.yml
...
