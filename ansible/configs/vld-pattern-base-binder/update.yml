---
- name: Update vld-pattern-base-binder
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Entering the vld-pattern-base-binder update.yml
      debug:
        msg:
          - Entering the vld-pattern-base-binder update.yml

    - name: Exiting the vld-pattern-base-binder update.yml
      debug:
        msg:
          - Exiting the vld-pattern-base-binder update.yml
...
