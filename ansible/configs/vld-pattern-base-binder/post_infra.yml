---
- name: Step 002 Post Infrastructure
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tags:
    - step002
    - post_infrastructure
  tasks:

    - name: Entering the vld-pattern-base-binder post_infra.yml
      debug:
        msg:
          - Entering the vld-pattern-base-binder post_infra.yml

    - name: Exiting the vld-pattern-base-binder post_infra.yml
      debug:
        msg:
          - Exiting the vld-pattern-base-binder post_infra.yml
...
