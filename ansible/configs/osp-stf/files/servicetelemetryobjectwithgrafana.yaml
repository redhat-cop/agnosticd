apiVersion: infra.watch/v1alpha1
kind: ServiceTelemetry
metadata:
  name: stf-default
  namespace: service-telemetry
spec:
  backends:
    metrics:
      prometheus:
        enabled: true
    events:
      elasticsearch:
        enabled: true
    graphing:
      enabled: true