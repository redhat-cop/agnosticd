
[example:children]
hosts

[hosts]
{% for host in groups['all'] %}
{{ hostvars[host]['private_hostname'] }} internal_hostname=master{{loop.index}}.{{chomped_zone_internal_dns}} ansible_ssh_user={{hostvars[host]['ansible_ssh_user']}} ansible_ssh_private_key_file={{hostvars[host]['ansible_ssh_private_key_file']}}
{% endfor %}
