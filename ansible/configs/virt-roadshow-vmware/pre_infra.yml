---
- name: Step 000 Pre Infrastructure
  hosts: localhost
  connection: local
  become: false
  gather_facts: false

  tags:
    - step001
    - pre_infrastructure

  tasks:

    - name: Entering the virt-roadshow-vmware pre_infra.yml
      debug:
        msg:
          - Entering the virt-roadshow-vmware pre_infra.yml

    - name: Exiting the virt-roadshow-vmware pre_infra.yml
      debug:
        msg:
          - Exiting the virt-roadshow-vmware pre_infra.yml
...
