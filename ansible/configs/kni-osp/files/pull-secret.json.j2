{# {{ my_pull_secret | to_json }} #}

{
  "auths": {
{% for key, value in {{ my_static_pull | json_query('auths') }} %}
    {{ key }}: {{ value }}
{% endfor %}
{%for key, value in {{ my_pull_secret | json_query('auths') }} %}
    ,{{ key }}: {{ value }}
{% endfor %}
  }
}
