- name: Delete Infrastructure
  hosts: localhost
  connection: local
  gather_facts: False
  become: no
  
    
    

- import_playbook: "{{output_dir}}/rhte-ansible-net/provisioner/teardown_lab.yml"
