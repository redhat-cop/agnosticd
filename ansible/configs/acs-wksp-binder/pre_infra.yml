---
- name: Step 000 Pre Infrastructure
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tags:
    - step001
    - pre_infrastructure

  tasks:
    - name: Entering the pre_infra.yml
      ansible.builtin.debug:
        msg: Entering the pre_infra.yml

    - name: Exiting the pre_infra.yml
      ansible.builtin.debug:
        msg: Exiting the pre_infra.yml
...
