---
- name: Step 003 Pre Software
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tags:
    - step003
    - pre_software
  tasks:
    - name: Entering the  pre_software.yml
      ansible.builtin.debug:
        msg: Entering the  pre_software.yml

    - name: Exiting the  pre_software.yml
      ansible.builtin.debug:
        msg: Exiting the  pre_software.yml

...
