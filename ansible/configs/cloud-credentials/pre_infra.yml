---
- name: Step 000 Pre Infrastructure
  hosts: localhost
  become: false
  gather_facts: false
  tags:
    - step001
    - pre_infrastructure
  tasks:
    - debug:
        msg: "Step 000 Pre Infrastructure"
