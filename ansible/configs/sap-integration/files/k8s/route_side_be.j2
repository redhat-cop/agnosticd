kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: sap-side-by-side-be
  namespace: side-by-side
  labels:
    app: sap-side-by-side-be
spec:
  host: sap-side-by-side-be.apps.cluster-{{ guid }}.{{ ocp4_base_domain }}
  to:
    kind: Service
    name: sap-side-by-side-be
    weight: 100
  port:
    targetPort: 8080-tcp
  wildcardPolicy: None