---
- name: Destroy playbook
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  tasks:
    - name: Entering the devsecops-binder destroy.yml
      ansible.builtin.debug:
        msg: Entering the destroy.yml

    - name: Exiting the devsecops destroy.yml
      ansible.builtin.debug:
        msg: Exiting the destroy.yml
...
