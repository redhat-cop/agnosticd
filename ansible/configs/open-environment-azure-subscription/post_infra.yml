---
- name: Step 003 Post Infrastructure
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tags:
  - step003
  - post_infrastructure
  tasks:
  - ansible.builtin.debug:
      msg: "Step 003 Post Infrastructure - Dummy action"
