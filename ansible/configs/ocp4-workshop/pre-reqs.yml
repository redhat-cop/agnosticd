- name: Step 000 Ensure pre-requisites
  hosts: localhost
  connection: local
  become: false
  tags:
    - step000
    - pre_requisites

  tasks:
    - name: Ensure admin_password is defined
      fail:
        msg: "admin_password must be defined"
      when:
        - ansible_password is not defined

# Any other prerequisites --> TBD
