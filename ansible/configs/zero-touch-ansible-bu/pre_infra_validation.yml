---

- name: Step 000 Pre Infrastructure Validation
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  tags:
    - step000
    - pre_infrastructure

  tasks:

    - name: Step 000 Pre Infrastructure Validation
      ansible.builtin.debug:
        msg: Validate Pre pre_infrastructure

...
