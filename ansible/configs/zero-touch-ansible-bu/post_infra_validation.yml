---

- name: Step 000 post Infrastructure Validation
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  tags:
    - step000
    - post_infrastructure

  tasks:

    - name: Step 000 post Infrastructure Validation
      ansible.builtin.debug:
        msg: Validate post post_infrastructure

...
