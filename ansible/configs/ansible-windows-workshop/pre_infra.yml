- name: Step 000 Pre Infrastructure
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  tags:
    - step001
  tasks:
    - debug:
        msg: "Step 000 Pre Infrastructure"
