---
- name: Step 005 post software
  hosts: localhost
  gather_facts: false
  become: true
  tags:
    - step005
    - post_software

  tasks:

    - name: "Post-Software tasks Started"
      ansible.builtin.debug:
        msg: "Post-Software tasks Started"

# ----------------------------------------------------------------------
# Post-Software Workloads as role
# ----------------------------------------------------------------------
- name: Import Post-software workloads
  import_playbook: workloads.yml
  vars:
    _workload_title_: "Post Software"
    _workloads_: "{{ post_software_workloads | default([]) }}"

- name: Execute task file based on a boolean condition
  hosts: bastions
  become: true
  become_user: instruct
  tasks:
    - name: Setup Instructlab
      when: instructlab_zt_setup | default(false) | bool
      include_tasks: instructlab_setup.yml


- name: showroom deployer
  hosts: localhost
  gather_facts: true
  become: true
  tags:
    - showroom
  module_defaults:
    group/k8s:
      host: "{{ sandbox_openshift_api_url }}"
      api_key: "{{ k8s_auth_results.k8s_auth.api_key|default(sandbox_openshift_api_key) }}"
      validate_certs: false
  environment:
    k8s_auth_api_key: "{{ sandbox_openshift_api_key  | default('') }}"
    k8s_auth_host: "{{ sandbox_openshift_api_url | default('') }}"
  tasks:
    - name: Deploy Showroom as pods on the namespace (cnv)
      when:
        - cloud_provider == "openshift_cnv"
        - ocp4_workload_showroom_content_git_repo is defined
      include_role:
        name: ocp4_workload_showroom
      vars:
        ocp4_workload_showroom_namespace: "{{ sandbox_openshift_namespace }}"

    - name: Wait for showroom pod to be ready
      kubernetes.core.k8s_info:
        kind: Pod
        namespace: "{{ sandbox_openshift_namespace }}"
        label_selectors:
          - "app.kubernetes.io/name=showroom"
        wait: true
        wait_condition:
          type: Ready
          status: "True"
        wait_timeout: "{{ ocp4_workload_showroom_namespace_wait_ready | default(600) }}"

- name: Deploy user setup
  hosts: localhost
  gather_facts: false
  become: true
  tags:
    - step005
    - post_software

  tasks:
    - name: Report Access Information for CNV
      when:
        - cloud_provider == "openshift_cnv"
        - "'bastions' in groups"
      vars:
        _bastion_inventory_name: "{{ groups['bastions'][0] }}"
        _bastion_ssh_password: "{{ hostvars[bastion_hostname]['zero_touch_rhel_user_user_password'] }}"
        _kubeadmin_password: "{{ hostvars[bastion_hostname]['kubeadmin_password']['content'] | b64decode }}"
      block:
        - name: Print access user info
          agnosticd_user_info:
            msg: |
              You can access your bastion via SSH:
              ssh {{ zero_touch_rhel_user_user_name }}@{{ openshift_cnv_ssh_address }} -p {{ hostvars[groups['bastions'][0]].bastion_ssh_port }}

              Enter ssh password when prompted: {{ hostvars[groups['bastions'][0]]['zero_touch_rhel_user_user_password'] }}

        - name: Save user data
          agnosticd_user_info:
            data:
              cloud_provider: "{{ cloud_provider }}"
              bastion_public_hostname: "{{ openshift_cnv_ssh_address }}"
              bastion_ssh_user_name: "{{ zero_touch_rhel_user_user_name }}"
              bastion_ssh_password: "{{ zero_touch_rhel_user_user_password }}"
              bastion_ssh_port: "{{ hostvars[groups['bastions'][0]].bastion_ssh_port  }}"
              ssh_command: "ssh {{ zero_touch_rhel_user_user_name }}@{{ openshift_cnv_ssh_address }} -p {{ hostvars[groups['bastions'][0]].bastion_ssh_port }}"
              ssh_password: "{{ zero_touch_rhel_user_user_password }}"
              ssh_username: "{{ zero_touch_rhel_user_user_name }}"
              guid: "{{ guid }}"
              targethost: "{{ groups['bastions'][0] | regex_replace('\\..*$') }}.{{ guid }}{{ subdomain_base_suffix }}"
              targetport: "{{ hostvars[groups['bastions'][0]].bastion_ssh_port }}"
              subdomain: "{{ guid }}{{ subdomain_base_suffix }}"
              subdomain_internal: "{{ chomped_zone_internal_dns | default('') }}"
          register: r_user_data

    - name: Output ssh user_info for ec2 based deploys
      when:
        - cloud_provider == "ec2"
      block:
        - name: Set ssh user_info
          agnosticd_user_info:
            msg: "{{ __user_info }}"
          loop:
            - "To Access development node via SSH:"
            - "ssh_command: ssh {{ zero_touch_rhel_user_user_name }}@{{ groups['bastions'][0] | regex_replace('\\..*$') }}.{{ guid }}{{ subdomain_base_suffix }}"
            - "Enter ssh password when prompted: {{ zero_touch_rhel_user_user_password }}"
            - ""
          loop_control:
            loop_var: __user_info

        - name: Save user data
          agnosticd_user_info:
            data:
              cloud_provider: "{{ cloud_provider }}"
              ssh_command: "ssh {{ zero_touch_rhel_user_user_name }}@{{ groups['bastions'][0] | regex_replace('\\..*$') }}.{{ guid }}{{ subdomain_base_suffix }}"
              ssh_password: "{{ zero_touch_rhel_user_user_password }}"
              ssh_username: "{{ zero_touch_rhel_user_user_name }}"
              guid: "{{ guid }}"
              targethost: "{{ groups['bastions'][0] | regex_replace('\\..*$') }}.{{ guid }}{{ subdomain_base_suffix }}"
              subdomain: "{{ guid }}{{ subdomain_base_suffix }}"
              subdomain_internal: "{{ chomped_zone_internal_dns | default('') }}"
          register: r_user_data


- name: cockpit deployer
  hosts: bastions[0]
  gather_facts: true
  become: true
  tags:
    - cockpit
  tasks:

    - name: Deploy Cockpit
      when: zero_touch_cockpit_installed | default(false) | bool
      block:
        - name: Install cockpit console
          ansible.builtin.package:
            name:
              - cockpit
              - cockpit-system
            state: present

        - name: Enable and start cockpit console service
          ansible.builtin.service:
            name: cockpit.socket
            enabled: true
            state: started

        - name: print out user.info
          agnosticd_user_info:
            msg: |
              Cockpit URL: https://{{ groups['bastions'][0].split('.')[0] }}.{{ subdomain_base }}:9090/
              Cockpit User name: {{ student_name }}
              Cockpit User Password: {{ student_password }}

        - name: Save user data
          agnosticd_user_info:
            data:
              cockpit_url: "https://{{ groups['bastions'][0].split('.')[0] }}.{{ subdomain_base }}:9090/"
              cockpit_user_name: "{{ student_name }}"
              cockpit_user_password: "{{ student_password }}"

    - name: Disconnect eth1 connection
      when: ec2_expand_network | default(false) | bool
      community.general.nmcli:
        conn_name: "Wired connection 1"
        state: absent

- name: Run post_software_validation playbook
  when: >-
    agd_post_software_validation | default(false) | bool
      or agd_validation | default(false) | bool
  ansible.builtin.import_playbook: post_software_validation.yml
  tags:
    - post_software_validation
    - agd_validation

- name: Lock Security Groups to Showroom and Ansible only
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  environment:
    AWS_ACCESS_KEY_ID: "{{ aws_access_key_id | default('') }}"
    AWS_SECRET_ACCESS_KEY: "{{ aws_secret_access_key | default('') }}"
    AWS_DEFAULT_REGION: "{{ aws_region_final | default(aws_region) | default('') }}"
  tasks:
    - name: Lock Bastion Security Group (EC2)
      when:
        - lock_bastion_security_group | default(false) | bool
        - cloud_provider == "ec2"
      ansible.builtin.include_tasks: lock_bastion_security_group.yml

    - name: Lock Bastion Security Group (CNV)
      when:
        - lock_bastion_security_group | default(false) | bool
        - cloud_provider == "openshift_cnv"
      ansible.builtin.include_tasks: lock_bastion_security_group_openshift_cnv.yml


- name: PostSoftware flight-check
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  tags:
    - post_flight_check
  tasks:

    - name: "Post-Software checks completed successfully"
      ansible.builtin.debug:
        msg: "Post-Software checks completed successfully"
...
