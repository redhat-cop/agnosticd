---
- name: Step 000 Pre Infrastructure
  hosts: localhost
  connection: local
  become: false
  gather_facts: false

  tags:
    - step001
    - pre_infrastructure

  tasks:

    - name: Entering the roadshow-vmware pre_infra.yml
      debug:
        msg:
          - Entering the roadshow-vmware pre_infra.yml

    - name: Exiting the roadshow-vmware pre_infra.yml
      debug:
        msg:
          - Exiting the roadshow-vmware pre_infra.yml
...
