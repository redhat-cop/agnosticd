The oe-cert.conf file and the vpn-cert-gen.py script are pre-installed on the ipsec.example.com machine.

To restart the lab exercise from scratch, run:

systemctl stop ipsec
rm -rf /etc/ipsec.*
rm -rf /root/labcerts
yum remove libreswan nss

