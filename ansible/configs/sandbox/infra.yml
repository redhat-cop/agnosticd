---
- name: Step 001 Infrastructure
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tags:
    - step001
    - infrastructure
  tasks:
    - debug:
        msg: "Step 001 Infrastructure - Starting"

    - when: cloud_provider == 'ec2'
      include_tasks: infra_ec2.yml
