---
- name: Update hybrid-cloud-binder
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Entering the hybrid-cloud-binder update.yml
      debug:
        msg:
          - Entering the hybrid-cloud-binder update.yml

    - name: Exiting the hybrid-cloud-binder update.yml
      debug:
        msg:
          - Exiting the hybrid-cloud-binder update.yml
...
