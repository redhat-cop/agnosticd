---

ibm_cloud_scenario: scenario_ibm_instructlab_service
  
# ibm_cloud_scenario: false

email: "jonsnow@example.com"

ibm_deploy_with_ansible: true
ibm_resource_prefix: "ibm_{{ guid }}"
ibm_name_prefix: "ibm-{{ guid }}"

ibm_resource_group_name: "{{ guid }}-resource-group"
ibm_access_group_name: "{{ guid }}-access-group"
ibm_trusted_profile_name: "{{ guid }}-trusted-profile"
ibm_cos_instance_name: "{{ guid }}-cos-instance"
bucket_name: "{{ guid }}-unique-bucket-name"

# ibm_realm_name: "iamasecret"
ibm_region_location: "us-south"
ibm_storage_class: standard 


# Terraform vars - this is experimental and not recommended for production use

ibm_deploy_with_terraform: false
ibm_terraform_dir: "{{ output_dir }}"

ibm_terraform_version: "1.51.0"
ibm_terraform_resource_prefix: "ibm_{{ guid }}"
ibm_terraform_name_prefix: "ibm-{{ guid }}"