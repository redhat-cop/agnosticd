apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: dbaas-operator
  namespace: "{{ ocp4_workload_cnd_rhoda_namespace }}"
spec:
  channel: "{{ ocp4_workload_cnd_rhoda_channel }}"
  name: "{{ ocp4_workload_cnd_rhoda_channel_name }}"
  source: "{{ ocp4_workload_cnd_rhoda_catalogsource_name }}"
  sourceNamespace: openshift-marketplace
