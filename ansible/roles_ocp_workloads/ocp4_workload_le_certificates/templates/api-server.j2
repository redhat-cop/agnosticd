apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  servingCerts:
    namedCertificates:
    - names:
      - {{ r_api_hostname.stdout }}
      servingCertificate:
        name: api-certs
