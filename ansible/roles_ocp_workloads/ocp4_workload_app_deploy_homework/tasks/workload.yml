---
- name: Setting up workload for user
  debug:
    msg: "Setting up workload for user ocp_username = {{ ocp_username }}"

# To Do: Implement your workload deployment tasks here
# -------------------------------------------------------------------------

- name: "Add user {{ ocp_username }} to groups {{ ocp4_workload_app_deploy_homework_ocp_user_groups }} to enable creating projects"
  command: "oc adm groups add-users {{ item }} {{ocp_username}}"
  with_items: "{{ ocp4_workload_app_deploy_homework_ocp_user_groups }}"

- name: Create ClusterResourceQuota and allow access to Grading Jenkins
  k8s:
    state: present
    definition: "{{ lookup('template', item ) | from_yaml }}"
  loop:
  - ./templates/cluster_resource_quota.j2
  - ./templates/jenkins_role_binding.j2

# Leave this as the last task in the playbook.
# --------------------------------------------
- name: workload tasks complete
  debug:
    msg: "Workload Tasks completed successfully."
  when: not silent|bool
