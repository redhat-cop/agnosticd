apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: odf-{{ _namespace }}-{{ cluster.name }}
  namespace: {{ _namespace }}
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  selector:
    matchLabels:
      app: odf-{{ _namespace }}-{{ cluster.name }}