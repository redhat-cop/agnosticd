apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  name: agnosticg
  namespace: {{ ocp4_workload_gitops_amqstreams_acm_namespace }}
spec:
  type: Git
  pathname: "{{ ocp4_workload_gitops_amqstreams_git_repo_url }}"