apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: "{{ ocp4_workload_minio_project }}"
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: minio
  sessionAffinity: None
  type: ClusterIP