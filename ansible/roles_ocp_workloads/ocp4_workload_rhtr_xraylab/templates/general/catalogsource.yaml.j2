apiVersion: operators.coreos.com/v1alpha1
kind: CatalogSource
metadata:
  name: "{{ catalogsource_name }}"
  namespace: "{{ catalogsource_namespace }}"
spec:
  sourceType: grpc
  image: "{{ catalogsource_name_snapshot_image }}:{{ catalogsource_name_snapshot_image_tag }}"
  displayName: "{{ catalogsource_displayname }}"