apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: acm-kafka-{{ _environment }}
  namespace: {{ ocp4_workload_devsecops_validated_pattern_kafka_namespace }}
spec:
  clusterSelector:
    matchLabels:
      purpose: "{{ _purpose }}"