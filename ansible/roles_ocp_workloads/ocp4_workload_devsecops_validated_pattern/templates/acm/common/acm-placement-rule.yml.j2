apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: devsecops-{{ _environment }}
  namespace: {{ item }}
spec:
  clusterSelector:
    matchLabels:
      purpose: "{{ _purpose }}"