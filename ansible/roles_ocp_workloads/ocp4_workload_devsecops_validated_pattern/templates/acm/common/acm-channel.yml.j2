apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  name: acm-devsecops
  namespace: "{{ ocp4_workload_devsecops_validated_pattern_acm_namespace }}"
  annotations:
    apps.open-cluster-management.io/reconcile-rate: high
spec:
  type: Git
  pathname: "{{ _ocp4_workload_devsecops_validated_pattern_gitea_repo_devsecops_url }}"