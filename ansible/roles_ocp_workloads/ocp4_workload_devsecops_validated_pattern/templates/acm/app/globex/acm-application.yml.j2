apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: acm-globex-{{ _environment }}
  namespace: openshift-gitops
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  selector:
    matchLabels:
      app: acm-globex-{{ _environment }}