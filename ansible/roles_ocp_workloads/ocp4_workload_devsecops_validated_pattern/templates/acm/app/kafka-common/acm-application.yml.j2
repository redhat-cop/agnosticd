apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: acm-kafka-common-{{ _environment }}
  namespace: kafka
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  selector:
    matchLabels:
      app: acm-kafka-common-{{ _environment }}