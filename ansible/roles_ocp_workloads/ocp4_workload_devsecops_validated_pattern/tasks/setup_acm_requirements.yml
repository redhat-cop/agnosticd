---
- name: Create ACM application set
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('template', item ) | from_yaml }}"
  loop:
  - templates/acm/acm-placement.yml.j2
  - templates/acm/acm-application-set.yml.j2
  vars:
    _app: globex

- name: Create ACM application set
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('template', item ) | from_yaml }}"
  loop:
  - templates/acm/acm-placement.yml.j2
  - templates/acm/acm-application-set.yml.j2
  vars:
    _app: kafka