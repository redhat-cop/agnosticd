---

- name: Set user info for showroom
  agnosticd_user_info:
    user: "{{ ocp4_workload_authentication_htpasswd_user_base }}{{ ocp4_workload_authentication_htpasswd_user_count }}"
    data:
      common_password: "{{ common_password }}"
