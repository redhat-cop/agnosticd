{% for __user in users %}
---
apiVersion: project.openshift.io/v1
kind: Project
metadata:
  name: {{ __user }}-mlops
  labels:
    kubernetes.io/metadata.name: {{ __user }}-mlops
    opendatahub.io/dashboard: 'true'
spec:
  finalizers:
    - kubernetes
{% endfor %}