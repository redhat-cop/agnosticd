---
# Implement your Workload deployment tasks here
- name: Setting up workload for user
  debug:
    msg: "Setting up workload for user ocp_username = {{ ocp_username }}"

# - name: Provision Ceph Object Storage
#   include_tasks: provision_ceph.yaml
#   vars:
#     storage_namespace: 'openshift-storage'

- name: Provision users workload
  include_tasks: provision_users_workload.yaml
  vars:
    storage_namespace: 'openshift-storage'

# - name: Provision shopper app
#   include_tasks: provision_shopper.yaml

# - name: Provision filestash S3 viewer
#   include_tasks: provision_filestash.yaml

# Leave this as the last task in the playbook.
- name: workload tasks complete
  debug:
    msg: "Workload Tasks completed successfully."
  when: not silent|bool
