apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: my-group
  namespace: openshift-storage
spec:
  targetNamespaces:
  - openshift-storage
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ocs-operator
  namespace: openshift-storage
spec:
  channel: "{{ ocp4_workload_serverless_ml_workshop_ocs_channel }}"
  installPlanApproval: Manual
  name: ocs-operator
  source: "{{ ocp4_workload_serverless_ml_workshop_catalogsource_name }}"
  sourceNamespace: openshift-storage
  startingCSV: "{{ ocp4_workload_serverless_ml_workshop_ocs_starting_csv }}"