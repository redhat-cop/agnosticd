apiVersion: maistra.io/v2
kind: ServiceMeshControlPlane
metadata:
  name: workshop-install
  namespace: {{ namespace }}
spec:
  version: v2.2
  proxy:
    networking:
      trafficControl:
        outbound:
          policy: REGISTRY_ONLY
  tracing:
    type: Jaeger
    sampling: 10000
  addons:
    jaeger:
      name: jaeger
    grafana:
      enabled: true
    kiali:
      name: kiali
      enabled: true
    prometheus:
      enabled: true