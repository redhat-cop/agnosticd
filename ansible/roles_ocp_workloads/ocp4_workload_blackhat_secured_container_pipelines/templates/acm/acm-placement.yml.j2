apiVersion: cluster.open-cluster-management.io/v1alpha1
kind: Placement
metadata:
  name: {{ _app_name }}-{{ cluster.name }}
  namespace: {{ ocp4_workload_blackhat_secured_container_pipelines_openshift_gitops_namespace }}
spec:
  clusterSets:
    - global
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - key: purpose
              operator: In
              values:
                - {{ cluster.purpose }}