---
- name: Save ssh data
  agnosticd_user_info:
    data:
      ssh_command: "ssh {{ student_name }}@bastion.{{ ocp4_workload_tap_wks_sno_setup_dev_guid }}.{{ sandbox_zone }}"
      ssh_password: "{{ student_password }}"
      ssh_username: "{{ student_name }}"
      targethost: "bastion.{{ ocp4_workload_tap_wks_sno_setup_dev_guid }}.{{ sandbox_zone }}"
