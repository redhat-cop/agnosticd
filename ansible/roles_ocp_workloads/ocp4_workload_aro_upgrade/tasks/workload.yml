---
# ocp4_workload_aro_upgrade_target_version: "4.18.23"
# ocp4_workload_aro_upgrade_channel: "stable-4.18"

- name: Get current ClusterVersion to check if already at target
  kubernetes.core.k8s_info:
    api_version: config.openshift.io/v1
    kind: ClusterVersion
    name: version
  register: _initial_cluster_version

- name: Check if upgrade is needed and run validation tasks
  when: (_initial_cluster_version.resources[0].spec.desiredUpdate.version.split('.') | map('int') | list) < (ocp4_workload_aro_upgrade_target_version.split('.') | map('int') | list)
#  when: _initial_cluster_version.resources[0].spec.desiredUpdate.version | version_compare(ocp4_workload_aro_upgrade_target_version, '<')
  block:

    - name: Update OpenShift channel to version {{ ocp4_workload_aro_upgrade_channel }}
      ansible.builtin.shell: |
        oc patch clusterversion version --type merge -p '{"spec": {"channel": "{{ ocp4_workload_aro_upgrade_channel }}"}}'

    - name: Get ClusterVersion resource
      kubernetes.core.k8s_info:
        api_version: config.openshift.io/v1
        kind: ClusterVersion
        name: version
      register: _cluster_version_info

    # - name: Assert that '{{ ocp4_workload_aro_upgrade_channel }}' is an available channel
    #   ansible.builtin.assert:
    #     that:
    #       - "ocp4_workload_aro_upgrade_channel in _cluster_version_info.resources[0].status.availableUpdates[0].channels"
    #     fail_msg: "Validation failed: '{{ ocp4_workload_aro_upgrade_channel }}' is not found in the list of available channels."

    - name: Create UpgradeConfig for {{ ocp4_workload_aro_upgrade_target_version }} and backdate to start immediately
      kubernetes.core.k8s:
        state: present
        definition: |
          apiVersion: upgrade.managed.openshift.io/v1alpha1
          kind: UpgradeConfig
          metadata:
            name: managed-upgrade-config
            namespace: openshift-managed-upgrade-operator
          spec:
            type: "ARO"
            upgradeAt: "2025-02-08T03:20:00Z"
            PDBForceDrainTimeout: 60
            desired:
              channel: "{{ ocp4_workload_aro_upgrade_channel }}"
              version: "{{ ocp4_workload_aro_upgrade_target_version }}"

    - name: Get ClusterVersion resource and wait for progression
      kubernetes.core.k8s_info:
        api_version: config.openshift.io/v1
        kind: ClusterVersion
        name: version
      register: _upgrade_cluster_version_info
      until: (_upgrade_cluster_version_info.resources[0].status.conditions | selectattr('type', 'equalto', 'Progressing') | map(attribute='status') | first | default('True')) | bool == false
      retries: 120
      delay: 60

    - name: Display specific ClusterVersion conditions
      ansible.builtin.debug:
        msg: "{{ item.type }}: {{ item.status }} - {{ item.message }}"
      loop: "{{ _upgrade_cluster_version_info.resources[0].status.conditions }}"
      when: item.type == 'Progressing' or item.type == 'Available'

    # - name: Wait for cluster to be Available and match the target version
    #   kubernetes.core.k8s_info:
    #     api_version: config.openshift.io/v1
    #     kind: ClusterVersion
    #     name: version
    #   register: _final_cluster_status
    #   until:
    #     - (_final_cluster_status.resources[0].status.conditions | selectattr('type', 'equalto', 'Progressing') | map(attribute='status') | first | default('True')) | bool == false
    #     - (_final_cluster_status.resources[0].status.conditions | selectattr('type', 'equalto', 'Available') | map(attribute='status') | first | default('False')) | bool == true
    #     - _final_cluster_status.resources[0].spec.desiredUpdate.version == ocp4_workload_aro_upgrade_target_version
    #   retries: 120
    #   delay: 60

    # - name: Wait for all Cluster Operators to be available
    #   kubernetes.core.k8s_info:
    #     api_version: config.openshift.io/v1
    #     kind: ClusterOperator
    #     name: "*"
    #   register: _cluster_operators
    #   until: (_cluster_operators.resources | map(attribute='status.conditions') | flatten | selectattr('type', 'equalto', 'Available') | map(attribute='status') | first | default('True')) | bool == true
    #   retries: 120
    #   delay: 60

    - name: Install matching OpenShift CLI oc
      set_fact:
        ocp4_client_url: >-
          {{ '{0}/ocp/{1}/openshift-client-linux-{1}.tar.gz'.format(
            ocp4_installer_root_url | default("https://mirror.openshift.com/pub/openshift-v4/clients"),
            ocp4_workload_aro_upgrade_target_version
          ) }}

    - name: Ensure ocp4_client_url is set
      assert:
        that: ocp4_client_url | default('') | length > 0

    - name: Install OpenShift CLI
      become: true
      unarchive:
        src: "{{ ocp4_client_url }}"
        remote_src: true
        dest: /usr/bin
        mode: 0775
        owner: root
        group: root
      retries: 10
      register: r_client
      until: r_client is success
      delay: 30

    # oc get clusterversion version -o json | jq -r '.status.conditions[] | select(.type == "Progressing" or .type == "Available") | "\(.type): \(.status) - \(.message)"'

    # test for the upgrade status to become Progressing for {{ ocp4_workload_aro_upgrade_target_version }}

    # # are nodes schedulable again?
    # $ oc get nodes -o jsonpath='{range .items[?(@.spec.unschedulable==true)]}{.metadata.name}{"\n"}{end}'
    # aro-cluster-mjb4l-dwx2w-master-2
    # aro-cluster-mjb4l-dwx2w-worker-eastus2-4j4vx


    # # Check cluster version
    # echo -e "\n1. Cluster Version:"
    # oc get clusterversion version -o json | jq -r '.status.conditions[] | select(.type == "Progressing" or .type == "Available") | "\(.type): \(.status) - \(.message)"'

    # Check if upgrade is complete
    # PROGRESSING=$(oc get clusterversion version -o json | jq -r '.status.conditions[] | select(.type == "Progressing") | .status')
    # AVAILABLE=$(oc get clusterversion version -o json | jq -r '.status.conditions[] | select(.type == "Available") | .status')
    #
    # echo -e "\n2. Operator Status:"
    # UNAVAILABLE_OPERATORS=$(oc get clusteroperators | grep -v "True.*True.*False" | grep -v NAME | wc -l)
    # echo "Unavailable operators: $UNAVAILABLE_OPERATORS"
    #
    # echo -e "\n3. Node Versions:"
    # oc get nodes -o json | jq -r '.items[].status.nodeInfo.kubeletVersion' | sort | uniq -c
    #
    # if [ "$PROGRESSING" = "False" ] && [ "$AVAILABLE" = "True" ] && [ "$UNAVAILABLE_OPERATORS" -eq 0 ]; then
    #     echo -e "\n✅ Upgrade is COMPLETE!"
    # else
    #     echo -e "\n⏳ Upgrade is still in progress or has issues"
    # fi
    #
    # - name: await application availability by querying the OpenShift API
    #   k8s_info:
    #     api_version: apps.openshift.io/v1
    #     kind: DeploymentConfig
    #     name: "httpd-example"
    #     namespace: "{{ ocp4_workload_example_dedicated_cluster_namespace }}"
    #   register: r_app_deploymentConfig
    #   retries: 50
    #   delay: 10
    #   until:
    #   - r_app_deploymentConfig.resources[0].status.readyReplicas is defined
    #   - r_app_deploymentConfig.resources[0].status.replicas is defined
    #   - r_app_deploymentConfig.resources[0].status.readyReplicas == r_app_deploymentConfig.resources[0].status.replicas
    #
    # - name: get route by querying the OpenShift API
    #   k8s_info:
    #     api_version: route.openshift.io/v1
    #     kind: Route
    #     name: "httpd-example"
    #     namespace: "{{ ocp4_workload_example_dedicated_cluster_namespace }}"
    #   register: r_app_route
    #
    # - name: Notify requestor of route.  CloudForms will see this and send email
    #   agnosticd_user_info:
    #     msg: "{{ item }}"
    #   loop:
    #   - ""
    #   - "Use the following route to access your application {{ r_app_route.resources[0].spec.host }}"
    #
    # # Leave this as the last task in the playbook.
    # # --------------------------------------------
    # - name: workload tasks complete
    #   debug:
    #     msg: "Workload Tasks completed successfully."
#   when: not silent|bool
