apiVersion: operators.coreos.com/v1alpha1
kind: InstallPlan
metadata:
  name: "{{ ocp4_workload_jenkins_jenkins_install_plan_name }}"
  namespace: "{{ ocp4_workload_jenkins_project }}"
spec:
  approved: true
