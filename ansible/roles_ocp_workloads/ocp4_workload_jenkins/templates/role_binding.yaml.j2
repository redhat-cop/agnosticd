apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: "{{ ocp4_workload_jenkins_name }}_edit"
  namespace: "{{ ocp4_workload_jenkins_project }}"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: edit
subjects:
- kind: ServiceAccount
  name: "{{ ocp4_workload_jenkins_name }}"
