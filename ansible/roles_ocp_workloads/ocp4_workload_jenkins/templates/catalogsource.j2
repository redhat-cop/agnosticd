apiVersion: operators.coreos.com/v1alpha1
kind: CatalogSource
metadata:
  name: "{{ ocp4_workload_jenkins_catalogsource_name }}"
  namespace: "{{ ocp4_workload_jenkins_project }}"
spec:
  sourceType: grpc
  image: "{{ ocp4_workload_jenkins_catalog_snapshot_image }}:{{ ocp4_workload_jenkins_catalog_snapshot_image_tag }}"
  displayName: "{{ ocp4_workload_jenkins_catalog_snapshot_image }}"