apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: web-ui-ci-3slya9
  namespace: coolstore
  labels:
    tekton.dev/pipeline: web-ui-ci
spec:
  params:
    - name: IMAGE_NAME
      value: image-registry.openshift-image-registry.svc:5000/coolstore/web-ui
    - name: GIT_REPO_URL
      value: "{{ }}"
    - name: COOLSTORE_WEB_URI
      value:  "{{ }}"
  pipelineRef:
    name: web-ui-ci
  serviceAccountName: pipeline
  timeout: 1h0m0s
  workspaces:
    - emptyDir: {}
      name: workspace
