---
apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  name: rhdp-helm
  namespace: rhdp-channels
  annotations:
    apps.open-cluster-management.io/reconcile-rate: medium
spec:
  type: Git
  pathname: {{ ocp4_workload_deploy_hosted_cluster_helm_chart_base_url }}
