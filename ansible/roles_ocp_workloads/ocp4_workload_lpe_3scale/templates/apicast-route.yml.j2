kind: Route
apiVersion: route.openshift.io/v1
metadata:
  namespace: "{{ocp4_workload_lpe_3scale_tenant_gw_project}}"
spec:
  host: >-
    {{ item }}-{{ ocp4_workload_lpe_3scale_tenant_gw_project }}.{{ ocp4_workload_lpe_3scale_ocp_apps_domain }}
  to:
    kind: Service
    name: {{ item }}
  port:
    targetPort: proxy
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
  wildcardPolicy: None
