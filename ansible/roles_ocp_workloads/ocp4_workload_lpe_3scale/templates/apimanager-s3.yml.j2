apiVersion: apps.3scale.net/v1alpha1
kind: APIManager
metadata:
  name: "{{ ocp4_workload_lpe_3scale_api_manager_name }}"
  namespace: "{{ ocp4_workload_lpe_3scale_namespace }}"
spec:
  wildcardDomain: "{{ guid }}.{{ ocp4_workload_lpe_3scale_ocp_apps_domain }}"
  resourceRequirementsEnabled: false
  system:
    fileStorage:
      simpleStorageService:
        configurationSecretRef:
          name: aws-auth
          