apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app.kubernetes.io/managed-by: EventListener
    app.kubernetes.io/part-of: Triggers
    eventlistener: {{ _pipeline_name_ }}-{{ _pipeline_dir_ }}-event-listener
  name: {{ _pipeline_name_ }}-{{ _pipeline_dir_ }}-event-listener
spec:
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: el-{{ _pipeline_name_ }}-{{ _pipeline_dir_ }}-event-listener
    weight: 100
  wildcardPolicy: None
