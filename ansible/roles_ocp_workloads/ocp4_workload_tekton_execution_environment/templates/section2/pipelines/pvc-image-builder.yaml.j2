apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ _name_ }}-pvc-image-builder"
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
