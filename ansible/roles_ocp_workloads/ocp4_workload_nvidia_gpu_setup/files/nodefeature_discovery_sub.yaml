---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: nfd
  namespace: openshift-nfd
spec:
  channel: "stable"
  installPlanApproval: "{{ 'Automatic' if nfd_operator_automatic_install_plan_approval else 'Manual' }}"
  name: nfd
  source: redhat-operators
  sourceNamespace: openshift-marketplace
