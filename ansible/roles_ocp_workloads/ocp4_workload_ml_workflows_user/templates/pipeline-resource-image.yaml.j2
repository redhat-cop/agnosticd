apiVersion: tekton.dev/v1alpha1
kind: PipelineResource
metadata:
  name: pipeline-image
spec:
  params:
    - name: url
      value: >-
        image-registry.openshift-image-registry.svc:5000/{{namespace}}/pipeline:latest
  type: image
