apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: serverless-workshop-role
rules:
  - verbs:
      - update
      # - delete
    apiGroups:
      - serving.knative.dev
    resources:
      - services