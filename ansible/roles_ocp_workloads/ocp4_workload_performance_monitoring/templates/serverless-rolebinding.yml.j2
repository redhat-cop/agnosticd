kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ user }}-serverless-role
  namespace: {{ user }}-staging
subjects:
  - kind: User
    name: '{{ user  }}'
    namespace: '{{ user }}-staging'
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: serverless-workshop-role
