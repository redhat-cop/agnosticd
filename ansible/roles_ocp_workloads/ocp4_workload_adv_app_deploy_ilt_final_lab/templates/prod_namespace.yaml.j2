apiVersion: v1
kind: Namespace
metadata:
  annotations:
    openshift.io/description: ""
    openshift.io/display-name: "Coffee Shop Production (GUID: {{ guid }})"
    openshift.io/requester: "{{ ocp_username }}"
  name: "{{ guid }}-prod-coffeeshop"
spec:
  finalizers:
  - kubernetes