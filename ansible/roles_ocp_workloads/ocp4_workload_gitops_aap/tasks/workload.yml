---

- name: Create secret in dev
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('template', 'argocd/clusterrolebinding.yml.j2' ) }}"

- name: Set up RHODS Object Detection PROD project  - argocd
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('template',  item ) | from_yaml }}"
  loop:
  - apps/aap-operator.yml
  - apps/aap-controller.yml
  - apps/aap-resources.yml
  - apps/patch-operator.yml