apiVersion: helm.openshift.io/v1beta1
kind: HelmChartRepository
metadata:
  name: nexus
spec:
 # optional name that might be used by console
  name: Nexus Helm Repository
  connectionConfig:
    url: http://nexus.nexus.svc:8081/repository/helm
