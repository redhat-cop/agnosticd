---
- name: Create patient-portal-frontend-deploy app on aws cluster
  environment:
    KUBECONFIG: "{{ tmp_kubeconfig }}"
  block:
    - name: deploy aws application
      kubernetes.core.k8s:
        state: present
        definition: "{{ lookup('template',  'aws_patient-portal-frontend-deploy.yaml.j2' ) | from_yaml }}"
