---
# Implement your Workload deployment tasks here
- name: Setting up workload for user
  debug:
    msg: "Setting up workload for user ocp_username = {{ ocp_username }}"

- name: Include podman task file
  become: true
  become_user: "{{ service_interconnect_bastion_username }}"
  when:
    - service_interconnect_pods_list is defined
    - service_interconnect_pods_install | default(false) | bool
  include_tasks: pods.yml

- name: Include AWS cluster tasks
  when:
    - service_interconnect_application is defined
    - service_interconnect_application == "aws"
  include_tasks: aws.yml

- name: Include Azure cluster tasks
  when:
    - service_interconnect_application is defined
    - service_interconnect_application == "azure"
  include_tasks: azure.yml

- name: Install skupper command
  include_tasks: skupper.yml


# Leave this as the last task in the playbook.
- name: workload tasks complete
  debug:
    msg: "Workload Tasks completed successfully."
  when: not silent | bool