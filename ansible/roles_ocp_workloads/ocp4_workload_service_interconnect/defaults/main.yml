---
become_override: false
ocp_username: user-redhat.com
silent: false
tmp_dir: /tmp/{{ guid }}
tmp_kubeconfig: "{{ tmp_dir }}/.kube/config"

# -----------------------------------------------------------------
# Service Interconnect Pods Variable
# -----------------------------------------------------------------
service_interconnect_bastion_username: "{{ student_name }}"
service_interconnect_bastion_groupname: users

service_interconnect_pods_list:
  - name: portal-database
    image: quay.io/redhatintegration/patient-portal-database:devnation
    ports:
      - '5432:5432'
  - name: portal-payments
    hostname: processed-at-datacentre
    image: quay.io/redhatintegration/patient-portal-payment-processor:devnation
    ports:
      - '8080:8080'
