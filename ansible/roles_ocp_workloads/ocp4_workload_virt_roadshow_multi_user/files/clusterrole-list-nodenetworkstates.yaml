apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: list-nodenetworkstates
rules:
  - apiGroups:
      - nmstate.io
    resources:
      - nodenetworkstates
    verbs:
      - list
