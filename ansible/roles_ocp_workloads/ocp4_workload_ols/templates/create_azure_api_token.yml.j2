kind: Secret
apiVersion: v1
metadata:
  name: azure-api-token
  namespace: openshift-lightspeed
  annotations:
    ols.openshift.io/watcher: cluster
stringData:
  main_client_id: "{{ _child_app_id }}"
  main_client_secret: "{{ _child_client_secret }}"
  tenant_id: "{{ ocp4_workload_ols_azure_tenant_id }}"
type: Opaque
