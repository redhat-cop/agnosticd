apiVersion: project.openshift.io/v1
kind: Project
metadata:
  annotations:
    openshift.io/description: ""
    openshift.io/display-name: "{{ ocp4_workload_integreatly_minio_namespace_display }}"
  name: "{{ ocp4_workload_integreatly_minio_namespace }}"
  labels: 
    integreatly: "true"
    middleware-key: "monitoring"
spec:
  finalizers:
  - kubernetes