= Allow OCP to be integrated with showroom (iframes)

* Patch IngressController to remove X-Frame-Option
* Create a new Route with Reencrypt and removing X-Frame-Option
* Set the quota to 0 for the pods on the authenticator operator namespace
