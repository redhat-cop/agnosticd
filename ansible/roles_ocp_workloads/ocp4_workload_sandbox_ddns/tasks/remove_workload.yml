---
- name: Delete TSIG key via bind-key-api
  ansible.builtin.uri:
    url: "http://{{ cluster_dns_server }}:8080/keys/cluster-{{ guid }}.{{ cluster_dns_zone }}"
    method: DELETE
    headers:
      Authorization: "Bearer {{ ocp4_workload_sandbox_ddns_token }}"
    status_code: [204]

