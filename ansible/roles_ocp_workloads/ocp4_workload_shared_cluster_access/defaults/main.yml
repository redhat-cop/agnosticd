---
become_override: false

silent: false


ocp4_workload_shared_cluster_access_username: ""
ocp4_workload_shared_cluster_access_email: ""
ocp4_workload_shared_cluster_access_password: ""

ocp4_workload_shared_cluster_access_keycloak_namespace: rhsso

ocp4_workload_shared_cluster_access_keycloak_realm:
  app: sso
  realm: rhdp

# All OpenShift groups to add the user to...
# Leave empty ( {} )to not add the user to any groups.
ocp4_workload_shared_cluster_access_user_groups: []

# Create a ClusterResourceQuota for user.
ocp4_workload_shared_cluster_access_create_quota: true

# ClusterResourceQuota. Must be in the format of a ClusterResourceQuota
# API resource (.spec.quota.hard)
ocp4_workload_shared_cluster_access_quota:
  hard:
    requests.cpu: "10"
    limits.cpu: "20"
    requests.memory: 6Gi
    limits.memory: 20Gi
    pods: "20"
    configmaps: "50"
    secrets: "150"
    services: "150"
    persistentvolumeclaims: "15"
    requests.storage: 50Gi
