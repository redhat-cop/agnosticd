apiVersion: quota.openshift.io/v1
kind: ClusterResourceQuota
metadata:
  name: "{{ ocp4_workload_shared_cluster_username }}"
spec:
  selector:
    annotations:
      openshift.io/requester: "{{ ocp4_workload_shared_cluster_username }}"
  quota: {{ ocp4_workload_shared_cluster_quota | to_json }}
