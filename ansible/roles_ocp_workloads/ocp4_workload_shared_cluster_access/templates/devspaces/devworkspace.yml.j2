apiVersion: workspace.devfile.io/v1alpha2
kind: DevWorkspace
metadata:
  name: {{ _devworkspace_name }}
  namespace: {{ _devworkspace_namespace }}
spec:
  routingClass: che
  started: true
  contributions:
    - name: ide
      uri: {{ _devspaces_devfile }}
  template:
    projects:
      - name: {{ ocp4_workload_shared_cluster_devworkspace_project }}
        git:
          remotes:
            origin: {{ ocp4_workload_shared_cluster_devworkspace_git_repo }}
          checkoutFrom:
            revision: {{ ocp4_workload_shared_cluster_devworkspace_git_revision }}
    components: {{ ocp4_workload_shared_cluster_devworkspace_components }}
