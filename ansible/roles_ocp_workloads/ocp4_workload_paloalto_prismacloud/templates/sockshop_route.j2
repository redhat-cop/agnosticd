kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: sockshop-route
  namespace: "{{ ocp4_workload_prisma_sockshop_project }}"
  labels:
    name: front-end
spec:
  to:
    kind: Service
    name: front-end
  port:
    targetPort: web