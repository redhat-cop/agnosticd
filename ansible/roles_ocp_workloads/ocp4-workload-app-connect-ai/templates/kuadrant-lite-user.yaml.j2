---
apiVersion: v1
kind: Secret
metadata:
  name: trlp-tutorial-api-key-lite-{{ __user }}
  namespace: kuadrant-system
  labels:
    authorino.kuadrant.io/managed-by: authorino
    app: my-llm-{{ __user }}
  annotations:
    kuadrant.io/groups: lite
    secret.kuadrant.io/user-id: "{{ __user }}"
stringData:
  api_key: "{{ __api_key }}"
type: Opaque
